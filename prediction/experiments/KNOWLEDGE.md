# KNOWLEDGE.md — Accumulated Research Learnings

The researcher agent reads this file FIRST every session. One entry per sub-stage: the single most important insight for building the final model. Newest entries at the bottom.

## Learnings

- [T1.1] (2026-08-26) Canonical fold design (in harness.py, use everywhere): 5 expanding folds with 2-year validation windows 2008-09 … 2016-17 — anchored at the last year where ≥50% of CMAs are active (2017), NOT the global max train year (2022), which yields degenerate Sherbrooke-only folds; data gotchas: X/y train CSVs align positionally, MDA needs a last-train-row anchor to score the first val month's direction, and evaluation.py's NMSE/NRMSE/NMAE normalize by row count n (MSE/n² etc.), not by scale.
- [T1.2] (2026-08-26) Harness VALIDATED and the bar is set: persistence reference under the 5 canonical folds = **(SUPERSEDED by T2.3 → MDA 0.6771±0.0474 under zero-drop scoring)** MDA 0.4587±0.0342, NMSE 1.52e-06±1.27e-06, NRMSE 0.00113±0.00049, NMAE 0.00061±0.00024 (per-fold values in artifacts/T1.2/, exact smoke-test match confirmed, prediction.py shift(1) convention verified identical on every scored row); two caveats all later work must respect: (1) upstream imputation (data_imputation.py ~L458-467) fits annual→monthly distribution + bidirectional interpolation on train+test JOINTLY before the split → CV absolutes may be optimistic; train-only re-imputation sensitivity check scheduled for T3.3/before T8; (2) fold-5 eval sets are protocol-dependent (317 rows for persistence-as-model vs 245 under a model blind to the 6 late-starter CMAs) — model-vs-baseline comparisons are only valid within a fold's common mask, never across folds.
- [T2.1] (2026-08-26) PERSISTENCE REMAINS THE BAR after the full simple-baseline suite: on identical folds/masks, drift MDA 0.4203±0.0384 / ARIMA(1,1,0) 0.4343±0.0396 / pooled ridge-on-lags 0.3710±0.0425 all LOSE to persistence on every aggregate metric (canonical reference now MDA **(SUPERSEDED by T2.3 → MDA 0.6771±0.0474 under zero-drop scoring)** 0.4625±0.0394 — supersedes T1.2's 0.4587±0.0342 because _anchored_mda was fixed to score unanchored within-window directions for CMAs with no train history; corrected row T1.2-baseline-validation-mdafix in runs.jsonl). Clarification of the T1.2 entry: "exact smoke-test match" certifies harness DETERMINISM/reproducibility only, not predictive strength. Substantive insights: NHPI total is near-martingale at monthly frequency — truth-refreshed recursive-update variants added later confirm it (recursive drift only TIES persistence, MDA 0.4624 vs 0.4587 paired, and recursive ARIMA(1,1,0) is WORSE than frozen ARIMA, 0.3843 vs 0.4343); estimated drift/AR extrapolation actively hurts at 12–24-month horizons. Ridge-on-lags matches persistence's error class on point metrics (NRMSE 0.00169 vs 0.00113) but has WORSE-than-persistence direction (MDA 0.371) → directional skill must come from target reformulation or different signal, motivating T2.2.

- [T2.2] (2026-08-26) TARGET FORMULATION DECIDED: model the LEVEL. On identical folds with ridge-on-lags as test vehicle and all predictions inverted to level space before scoring, reformulating the target does NOT buy directional skill: Δlog MDA 0.3715±0.0404 ≈ level 0.3710±0.0425 (delta +0.0005) and YoY% 0.3325±0.0424 is strictly worse; point errors also worsen under transforms (NRMSE 0.00197 Δlog / 0.02015 YoY vs 0.00169 level vs persistence 0.00113). No target form improved MDA over persistence → primary decision criterion fails for both alternatives, so keep LEVEL (simplest inversion-free path, best point-error class). Implication for later stages: directional skill must come from features/model class or forecast-horizon design, not target algebra; any future Δlog-style residual modeling must beat this head-to-head evidence.

- [T2.3] (2026-08-26) CANONICAL MDA REFERENCE CORRECTED + SPATIAL POOLING VERDICTED NEGATIVE. (1) MDA protocol fix: ~40% of monthly NHPI directions are exactly 0 (flat index runs), and the old scorer counted predicted-zero directions as automatic misses — a systematic anti-persistence bias concentrated at the train→val anchor; harness.mda_zero_drop now drops zero directions SYMMETRICALLY for model and baseline. NEW CANONICAL persistence reference = **MDA 0.6771±0.0474** (NMSE 1.52e-06 / NRMSE 0.00113 / NMAE 0.00061 bit-identical); every headline model MDA shifted up (drift 0.6852, arima 0.6682, ridge 0.6051, drift-rec 0.6758, arima-rec 0.5460, dlog 0.6059, yoy 0.5416 — corrected `-mdazero` rows appended); rankings unchanged, "persistence is the bar" stands (drift's nominal MDA edge comes with 40× worse point errors). Direct-sign diagnostic: raw Δlog/YoY heads DO have above-chance direction signal (0.608/0.581) but below momentum sign(y_{t−1}−y_{t−2}) (~0.678) — earlier "REFUTED" softened accordingly; LEVEL target unchanged. (2) LOCO probe: pooled ridge trained WITHOUT a held-out CMA loses to persistence on 13/18 full-history CMAs (mean ΔMDA −0.087; NRMSE 2.8× worse), beats self-fit ridge only marginally (+0.019 MDA, halved NRMSE — self-fit overfits short histories, NRMSE up to 0.53); no history-depth structure (corr −0.10). SPATIAL POOLING DOES NOT HELP at this scale → T3 recommendation: temporal features first (T3.2), kNN spatial-lag/region/centroid features demoted to cheap-ablation status (T3.1), with the caveat that LOCO tests out-of-CMA transfer of lag models, not neighbour-lag features per se.

- [T3.1] (2026-08-26) kNN NEIGHBOUR-LAG FEATURES: MARGINAL KEEP FOR T6 ABLATION ONLY — NOT A DIRECTION FIX. Centroid kNN (k=3/k=5, great-circle, Census-2021 shapefile `lcma000b21a_e.shp`) backward-shift features (neighbour mean lag-1 level & Δ, uniform + inverse-distance-weighted) added to the pooled ridge-on-lags vehicle move paired intersection-mask ΔMDA by only +0.0006 (k=3) / +0.0077 (k=5) on 3/5 folds each with point errors ~1.5% BETTER (NRMSE ratio 0.98) — they satisfy the pre-stated keep rule's letter, so they survive INTO T6 ablation screening, but both models stay ~0.06 MDA below persistence (0.604/0.613 vs 0.6771): neighbour pasts carry almost no signal beyond a CMA's own lag-1 (feature-vs-own-lag1 corr 0.48–0.56 for levels, ~0.01–0.07 for Δs). Also shipped: intersection-mask paired scorer (`harness.paired_intersection_scores`) — drift's nominal zero-drop MDA edge over persistence SURVIVES and widens under the stricter shared-direction pairing (Δ +0.0254, 3/5 folds; but NRMSE ~5× worse on the same rows, so the joint verdict is unchanged); recursive-drift vs persistence ΔMDA is EXACTLY 0 on every fold (adding a constant mu_c preserves predicted directions — it is directionally identical to momentum); the 853/805/406-of-2039 zero decomposition is now computed by committed code in mda_correction_summary.json. (CORRECTION, T3.2 critic fix #2: the drift "SURVIVES/proves" wording is downgraded — the edge is sign-consistent under stricter pairing (+0.0254 mean, 3/5 folds) but not statistically established (|t|<1), and jointly irrelevant regardless with NRMSE ~5× worse on the same rows; also, paired_intersection_scores had an anchor bug fixed in T3.2 — persistence-containing pairs unchanged, corrected k3/k5 deltas in artifacts/T3.1/decision_rule.json.)

- [T3.2] (2026-08-26) TEMPORAL FEATURES: NO DIRECTIONAL SIGNAL FROM ANY FAMILY, but the OWN-target block is a major POINT-ERROR lever. 34 strictly causal engineered features (own lags {3,6,12,24}, diffs d1/d12, Δlog1, momentum sign; SCSS starts/completions + RMS rent/vacancy rolling mean/std {12,36}+diff12; Census value/income diff12+rolling means) added incrementally to pooled ridge(α=1)-on-lags: NONE improves mean paired intersection-mask ΔMDA over lag1-only (OWN −0.0129 1/5 folds, ALL −0.0171 1/5 — fold-1 2008–09 hurts most), yet OWN alone cuts NRMSE 22% (0.00169→0.00131, ratio 0.778) and SCSS/RMS/Census add nothing at all → housing-quantity/rent covariates carry no incremental monthly signal; T6 forward selection should screen ONLY own-index long-horizon memory (tot_d12/tot_lag12/tot_lag24/tot_dlog1) for MDA-neutral point-error gains under the multi-metric bar. Also shipped all five T3.1 critic fixes: the paired-scorer ANCHOR BUG fix leaves persistence-containing pairs bit-identical (drift Δ+0.0254 etc.) but corrects k3/k5 spatial deltas to −0.0009 (now FAILS keep rule) / +0.0100 (still marginal keep).

## Standing decisions

- Target formulation (level vs Δlog vs YoY): **LEVEL — decided empirically in T2.2** (Δlog and YoY% give no directional gain and worse point errors; see T2.2 learning below)
- Validation scheme: expanding-window CV over years, grouped by CMA (defined once in harness.py, T1)
- Metrics: MDA, NMSE, NRMSE, NMAE (reuse evaluation/evaluation.py); persistence baseline always reported alongside
- Registry discipline: runs.jsonl is STRICTLY APPEND-ONLY (duplicates kept, superseded rows never deleted); duplicate run_ids ARE possible under append-only — downstream tooling MUST dedupe by (run_id, timestamp)
- Canonical MDA protocol: harness.mda_zero_drop (boundary-anchored, symmetric zero-direction drop); canonical persistence reference = MDA 0.6771±0.0474 / NMSE 1.52e-06±1.27e-06 / NRMSE 0.00113±0.00049 / NMAE 0.00061±0.00024; paired sensitivity via harness.paired_intersection_scores
- Multi-metric bar (T3.2 critic fix #2, EXPLICIT): a directional edge alone never constitutes "better" — a model only counts as beating persistence if it wins on MDA AND does not degrade the point-error class materially (>10% worse NRMSE disqualifies); per-fold signs of any claimed edge must be reported honestly

## Open questions

- Does spatial pooling help at this scale? **ANSWERED in T2.3: NO** (see T2.3 learning)
- Do spatial-lag features from the CMA shapefile add value over temporal features alone? (T3/T6 — deprioritised to ablation status by the T2.3 verdict)
